{
  "name": "ridwan-portfolio",
  "description": "Personal portfolio — Next.js + MDX",
  "keywords": [
    "Next.js",
    "MDX",
    "Portfolio"
  ],
  "homepage": "https://ridwansatria.com",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend",
    "format": "prettier --cache --write .",
    "lint": "npm run lint --workspace=frontend",
    "type-check": "npm run type-check --workspaces"
  },
  "devDependencies": {
    "npm-run-all2": "^5.0.2",
    "prettier": "^3.6.2"
  }
}